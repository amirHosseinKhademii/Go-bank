apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bank-app

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - ingress.yaml

labels:
  - pairs:
      app: bank-app
      managed-by: kustomize
