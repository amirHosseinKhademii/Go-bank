apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bank-app

resources:
  - namespace.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app: bank-app
  managed-by: kustomize
